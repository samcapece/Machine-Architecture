1. Samuel Capece
2. No I did not
3. No other notes
4. The contains and codebook methods run in O(Log n)
as they both use the Binary Search method to find the given char.
5. The decode method runs in O(b) as it checks
each individual bit, so its complexity is based linearly
with the number of bits given. 